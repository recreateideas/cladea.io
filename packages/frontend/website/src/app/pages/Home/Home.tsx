import { Sidebar } from "src/app/features";
import { Container } from "./styledComponents";

export const Home = () => {
  return (
    <Container id="home-page">
      <Sidebar />
    </Container>
  );
};
